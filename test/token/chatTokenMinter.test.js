// test adding and removing a minter address
// test claiming chat tokens from ChatTokenClaim using IggyPostEnumeration data
  // should fail when user has already claimed
  // should fail if user has no tokens to claim
// test minting chat tokens when minting a new post NFT

const { expect } = require("chai");